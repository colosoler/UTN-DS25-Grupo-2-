server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Sin esto, rutas como /materiales devuelven 404 porque nginx busca
        # el archivo físico. try_files sirve index.html y React Router toma el control
        try_files $uri $uri/ /index.html;
    }
}
